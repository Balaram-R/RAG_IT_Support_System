IT Support Knowledge Assistant

A local Retrieval-Augmented Generation (RAG) application that answers IT and network troubleshooting questions using ONLY the documents in the docs/ folder. The assistant combines semantic search, cross-encoder re-ranking, and a local LLM (Ollama) with strict guard rails so every answer is grounded in the knowledge base.

The model never answers from its own training knowledge: if the docs do not cover the question, the system refuses instead of guessing.

Features
•	Answers only from docs/ - the knowledge base is the model's only source of truth
•	8-stage pipeline with live LED status indicators in the UI
•	Cross-encoder re-ranking (ms-marco-MiniLM-L-6-v2) for retrieval quality
•	Grounding check - every answer unit is verified against the docs
•	Confidence score (0-100) with HIGH / MEDIUM / LOW labels
•	Guard rails: prompt-injection blocking, network-domain gate, retrieval gate
•	ChromaDB vector database (47 chunks from 10 documents)
•	Local LLM via Ollama (qwen2.5:3b) - offline, no external API costs
•	Streamlit web interface

Use Cases
•	Data From Docs



Technology Stack

•	Python 3.12
•	Streamlit
•	LangChain (community + text splitters)
•	ChromaDB
•	Ollama (qwen2.5:3b)
•	Sentence Transformers (all-MiniLM-L6-v2 embeddings + ms-marco cross-encoder reranker)



Project Structure
RAG_IT_Support_System
│
├── app.py            # Streamlit UI (LED pipeline, confidence, sources)
├── ingest.py         # Builds/rebuilds chroma_db from docs/
├── rag_engine.py     # 8-stage pipeline + guard rails
├── requirements.txt
├── Readme.docx
├── .gitignore
│
├── docs/             # Knowledge base (Input Documents)
├── chroma_db/        # Vector database (generated by ingest.py)
└── rag_errors.log    # Runtime warnings/errors




Installation
Create Virtual Environment
python -m venv .venv

Activate Virtual Environment
Windows:
.venv\Scripts\activate

Install Dependencies
pip install -r requirements.txt

Install Ollama Model
ollama pull qwen2.5:3b

Ollama must be running (start the Ollama app or run `ollama serve`). The engine connects to http://127.0.0.1:11434.

Build Vector Database
python ingest.py

Launch Application
streamlit run app.py



Architecture - 8-Stage Pipeline
Question
   │
   ▼
1. Input validation     - injection blocking + network-domain gate
   │
   ▼
2. Query embedding      - all-MiniLM-L6-v2
   │
   ▼
3. ChromaDB retrieval   - top 15 candidates
   │
   ▼
4. Re-ranking           - ms-marco cross-encoder, keep top 5
   │
   ▼
5. Context assembly     - top chunks from docs/
   │
   ▼
6. Ollama generation    - qwen2.5:3b (only if retrieval gate passed)
   │
   ▼
7. Grounding check      - verify answer units against docs
   │
   ▼
8. Confidence scoring   - 0-100 with HIGH / MEDIUM / LOW
   │
   ▼
Answer + sources
Guard Rails

Prompt-injection blocking - patterns such as "ignore previous instructions" are blocked at input validation.

Network-domain gate - the KB only covers network support; questions with no network terms are refused before retrieval (e.g. "my computer is slow").

Retrieval gate - if the best matching chunk scores below 0.40 similarity, the LLM is never called; the system refuses with "I don't have information about this in the knowledge base."

Grounding check - every answer unit is verified against the retrieved docs (lexical check for short units, embedding similarity >= 0.42 for prose). Ungrounded tails are trimmed; fully ungrounded answers are rejected.

Cautious-refusal retry - if the model refuses despite relevant docs, it retries once with a nudge prompt.


Author
Balaram R
